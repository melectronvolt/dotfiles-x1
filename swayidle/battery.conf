# Batterie : 2 min DPMS, 5 min lock, 10 min suspend, lock before-sleep
timeout 120 'swaymsg "output * power off"' resume 'swaymsg "output * power on"'
timeout 300 '~/.local/bin/sway-lock'
timeout 600 'systemctl suspend -i'
before-sleep '~/.local/bin/sway-lock'
